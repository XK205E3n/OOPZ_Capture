# 部署变更记录

本文件只记录会影响构建、配置、部署、运行、数据或回滚的变更；普通业务修复仍应由 Git 提交记录。不得写入密钥或隐私数据。

## 待发布

### 2026-09-12 — 部署：已有服务器一键更新到最新正式版

- 部署指南新增第 12.1 节，完整嵌入 update_latest_release.ps1：查询 GitHub Latest 正式版，校验标签提交、GitHub 资产摘要、旁置 SHA-256 和清单，调用新包安装器并核对 current。
- 已是最新版不重装、不重启；拒绝降级、活动任务/分析锁、遗留目标版本目录及不完整的 Release 元数据。保留 shared，提供仅下载准备模式，不自动合并配置契约变化。
- 验证：脚本文档一致性和模拟流程检查通过；本机隔离目录实际匿名下载、校验 v0.11.13 并完成 PrepareOnly，未在服务器执行升级；全量 220 passed、1 skipped，发布审计无新增命中。
- 不新增环境变量或迁移业务数据；本次只推送脚本与在线文档，v0.11.13 ZIP 不变。回滚本脚本不改变已安装版本或共享数据。

## 0.11.13 — 2026-09-12

- 正式包提交 `9bf093db9a6e`；在线部署指南及准备脚本同步固定版本与 SHA-256，构建复测 218 passed、1 skipped。

Release：[v0.11.13](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.13)。

### 2026-09-12 — 分析：内容审核拦截时拆分一次并标明缺失

- API 客户端识别 data_inspection_failed（含 OpenCode Go 的上游错误包装），只保留安全错误码，不输出错误响应中的原文。
- 单个短窗口首次被拦截时按时间中点拆分一次，正常 300 秒窗口为两个 150 秒片段。半段仍被同类错误拦截则跳过；两个均失败则均跳过。无文本半段不调用 API，普通 400、认证失败和超时仍按原失败逻辑处理，不递归拆分。
- 保存各半段时间范围/状态及缺失列表，内部、公开和简版报告直接渲染缺失说明。保留原转写、成功窗口缓存，完整摘要由成功半段按顺序合并，无额外合并请求；边界跨越的句子在两半保留完整文字。
- 使用量按原请求及各半段计数；被拒绝请求的 Token/费用未返回时，不声称费用估算完整。报告格式升至 3.8.0，不改变成功短摘要缓存指纹。
- 无新增配置或数据迁移；升级后重试原会话，跳过状态会缓存。回滚旧版仍可读取已生成报告，但旧程序不具备拆分处理能力。服务器未自动升级。
- 验证：覆盖两半成功、单边/双边跳过、空半段、其他错误失败、报告说明与缓存复用的回归测试通过；全量 218 passed、1 skipped，发布审计无新增命中。

## 0.11.12 — 2026-09-12

Release：[v0.11.12](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.12)。

### 2026-09-12 — 修复：静音分片阻断分析初始化

- continuous.no_text_marker 原先写入非 UUID 的 no-speech，分析器严格 UUID 校验导致包含静音分片的会话无法初始化。新记录改用稳定 UUID；analyzer_job 仅对匹配旧版静音标记特征的记录进行内存归一化，保留其他无效 ID、重复记录和时间范围校验。
- 旧转写文件、文本、时间戳及指纹输入不改写，无需重录或数据迁移；升级后从“待分析”重试原会话。若服务器错误行并非旧静音标记，仍需根据实际字段进一步诊断。
- 无配置变化。回滚旧版本会恢复其对旧静音记录的拒绝行为；共享原始数据不受影响。
- 验证：静音分片生成、合并、旧数据稳定读取与原文件保留、异常 ID 和重复记录拒绝专项测试通过；发布构建 209 passed、1 skipped，审计通过；在线下载入口固定为 v0.11.12、提交 `85ec1c9d4643` 及 SHA-256。

## 0.11.11 — 2026-09-12

- 发布包提交 `3f077e0ddc4d`；在线部署指南与准备脚本固定到新版 ZIP 和 SHA-256，构建复测 202 passed、1 skipped。无服务器自动部署。

Release：[v0.11.11](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.11)。包含安装器编码修复与最低 4 vCPU / 8 GiB 部署要求；无配置或数据迁移。

### 2026-09-12 — 修复：PowerShell 5.1 安装健康检查误判

- 原 v0.11.10 安装器无 BOM UTF-8 中文健康标记在 Windows PowerShell 5.1/代码页 936 下被误读，成功日志不能匹配，触发停机及首次安装 current 撤回；重复安装随后被目录存在保护拒绝。
- install_release.ps1 改用 ASCII 源码中的 Unicode 码点构造标记，保留仅检查本次新增日志的逻辑；新增原生 PowerShell 回归测试。部署指南第 9.5 节补充严格前提下恢复首次启动入口的步骤，并登记用户恢复正常的结果。
- 不新增配置、不迁移数据；现有 v0.11.10 ZIP 不变，安装器修复纳入 v0.11.11。已正常运行的实例无需重装；回滚安装器会恢复编码隐患，共享数据不受影响。
- 验证：原发布包误解码已复现，修复后真实 UTF-8 就绪日志匹配、空日志与失败日志拒绝检查通过；全量 202 passed、1 skipped，发布审计无新增命中。

### 2026-09-12 — 文档：最低部署要求统一为 4 vCPU / 8 GiB

- README、云部署指南、部署说明、运维说明和状态基线统一最低 CPU/内存要求，移除低配可部署或试运行建议；保留历史测量和版本记录，不再作为低配部署依据。
- 验证：全文检索与差异检查确认现行部署建议一致；仅文档修改，未变更代码、环境变量或已发布 ZIP，无数据迁移或运行时回滚影响。

## 0.11.10 — 2026-09-11

Release：[v0.11.10](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.10)。本版本包含以下此前未发布修复；不代表新服务器已验收。

### 2026-09-11 — 部署：新版下载入口

- 在线指南与 prepare_release.ps1 固定到 v0.11.10、提交 `351fee9b773b` 及构建后的 SHA-256，保留 v0.11.9 专用恢复段落及历史记录；构建快照中的旧下载入口不用于新安装。
- 发布包由干净提交生成，构建复测 201 passed、1 skipped；安装仍需新服务器端到端验收，无数据迁移。

### 2026-09-11 — 修复：云部署与百炼分析可用性

- 客户端为百炼官方兼容端点的 qwen3.8-flash 显式映射 enable_thinking，disabled/auto 不再意外启用思考，保留其他提供商行为；补充参数回归覆盖。
- 正式安装器增加 pip 官方源、隔离用户配置和重试，npm/pnpm 有界网络重试与环境恢复，补充依赖一致性和报告模块检查；包含此前 Chromium 安装及启动门槛。
- 部署文档补充 401 与超时诊断、账户使用范围、180 秒配置建议和旧版恢复边界；更新新服务器 4 vCPU/8 GiB 状态，并区分云端待验收项。云配置单独备份后只调整超时，不进入 Git。
- 验证：客户端专项 31 项通过；修复后合成材料 API 请求 HTTP 200、有效 JSON、无思考内容，耗时 52.8 秒，说明仍有等待波动。全量 201 passed、1 skipped，PowerShell 脚本语法检查通过；发布审计无新增命中。
- 部署影响：需要新发布包；无新增必填变量或数据迁移。共享配置不自动覆盖，超时需显式调整。回滚旧程序会恢复 Qwen 参数缺失行为，已下载浏览器及共享数据保留。

### 2026-09-11 — 录音浏览器成为安装与就绪门槛

- 新安装/续装流程使用目标 Python 执行 `playwright install --no-shell chromium` 并验证 `channel='chromium'` 无头启动，失败时不激活或标记就绪。
- 不把系统 Edge/Chrome 当作录音浏览器已安装；旧 v0.11.9 包不变，需按在线指南第 9.4 节补装。缓存归属运行账户，切换账户或 Playwright 版本后重新验证。
- 无 `.env` 契约或数据迁移；回滚脚本不会删除已下载浏览器、共享数据或缓存。
- 验证：191 passed、1 skipped；本机按 SDK 的 Chromium 通道实际启动成功，文档命令与脚本已检查。旧包未覆盖或重发，服务器仍按第 9.4 节补装后验收。

### 2026-09-11 — 保留已完成 Python / 模型阶段，续装 Node

- 用户日志确认 Python 依赖与模型已完成，失败点是 npm registry 获取 pnpm 时 ECONNRESET。新增限定 v0.11.9 首次安装的 Node 续装入口，校验版本源文件后在现有目录继续，保留 Python、模型和下载缓存。
- 仅临时覆盖 npm 配置路径、registry、重试和并发；保留 TLS、锁文件和禁用安装脚本。退出恢复环境，不清空全局缓存，不重建或删除版本目录。
- 所有依赖检查通过才生成 first-start-ready 标记，网关启动和连接验收为独立步骤；没有 current 的前提在启动时再次检查。
- 无 `.env` 契约或数据迁移；回滚准备脚本不会卸载依赖或删除共享数据。新辅助脚本通过在线文档提供，不改动既有发布包。
- 验证：191 passed、1 skipped，文档代码一致性与 16 段 PowerShell 语法检查通过；本机隔离目录实际安装 Node 依赖成功，服务器网络与导入仍待执行验证。

### 2026-09-10 — 首次 pip 依赖失败的安全重试

- 新增恢复入口与部署说明，限定已校验 v0.11.9 首次安装、尚无 `current` 且无目标运行进程的场景。先验证解释器及官方 PyPI 的兼容 NumPy 候选，再将失败目录移动到同安装根下 `failed-installs` 保留，随后调用原版安装器。
- 重试期间仅隔离进程级 `PIP_*` 与 pip 配置文件，指定官方 PyPI、超时与独立缓存；退出恢复原环境，不修改全局 pip.ini，不禁用 TLS 校验，不向配置文件写入凭据。
- 无数据迁移，`.env`、模型、会话和日志保持在 shared；不删除旧失败环境，其内部硬链接与目录联接保留。若再次失败，保留新错误与备份，不能把脚本通过的本机模拟当作云端安装成功。
- 当前发布包不变，新增恢复代码需从在线文档或脚本获取；以后版本或升级故障应单独审查，不套用此限定首装方案。
- 验证：恢复及文档同步专项测试通过，官方 PyPI 的 NumPy wheel dry-run 通过；全量 189 passed、1 skipped（浏览器测试另有原生诊断日志，未作运行健康承诺）。云端 pip 问题具体原因和重试结果仍待服务器执行确认。

### 2026-09-10 — 服务器不再安装 Git / GitHub CLI

- 匿名 Release 下载不依赖 Git 客户端或 gh，基础环境脚本与文档已移除对应安装和版本检查；不卸载已有软件，不影响开发机 Git 工作流。
- 无应用配置、数据或运行版本变化。回滚脚本会恢复旧的工具安装行为，但不删除现有环境；服务器继续使用在线指南中的新脚本。

### 2026-09-10 — 匿名获取发布包及终端部署

- 当前仓库可公开读取，匿名下载已实测通过；本次未变更可见性。准备脚本校验固定 Release 的 ZIP、旁置 SHA-256、内置摘要和提交，提取到独立准备目录并复制管理脚本，不需要 GitHub 登录或仓库克隆。
- 新增终端配置脚本，保留现有非空值，敏感输入不回显，配置文件原地写入以保留硬链接。服务器可先创建独立配置虚拟环境，直接调用已发布的一键飞书配置实现；正式版本安装方式不变。
- 新增脚本未写入已发布 v0.11.9 包，首次安装可复制在线指南代码；不新增环境变量，不迁移已有业务数据。
- 回滚仅回滚说明与准备脚本，不删除下载包、配置或已安装程序；未来升级须同步审核固定下载版本、提交和摘要。用户登录启动任务提供 PowerShell 注册示例，既有任务不覆盖；无人登录运行需另行验证。
- 验证：187 passed、1 skipped；13 段 PowerShell 语法检查、脚本与文档同步检查通过，正式附件匿名下载与重复准备已实测；未在来宾系统实际安装。

### 2026-09-10 — 缺失才安装的 Windows 基础环境引导

- 新增 `scripts/install_prerequisites.ps1` 并完整嵌入从零部署指南；从官方渠道下载缺少的 Visual C++ x64 运行库、Git、gh、Python 3.12、Node/npm/npx 和 Chrome，已有组件跳过，提供仅检查模式和签名检查。
- Python 通过官方安装管理器获取 3.12 x64，保留其他版本；首次安装时为新目录配置 PATH，输出供发布安装使用的精确解释器路径。准备已有部署流程要求的共享 Node 可执行文件，不覆盖既有文件。
- 配置/迁移：不变更 `.env` 契约或应用数据。仅在用户主动执行引导脚本时安装系统级基础软件；不自动重启、不登录账户、不部署应用。现有 v0.11.9 发布包保持不变，用户可以复制在线文档代码先准备环境。
- 回滚：本次文档/脚本回滚不卸载已经安装的基础软件；不自动删除共享目录或其他 Python 版本，软件维护通过 Windows 安装管理完成。
- 验证：184 passed、1 skipped；脚本/文档代码一致、重复执行与失败分支已测试，官方下载地址可访问；尚未在云服务器实际安装。

## 0.11.9 — 2026-09-10

Release：[v0.11.9](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.9)。包内清单记录精确构建提交。

### 2026-09-10 — 文档与试运行基线同步

- 修改：README 按实际模块和处理链路整理，补齐本地 Python / Node / 模型准备；飞书一键配置作为主流程，手动配置仅作保底；说明和配置模板仅推荐 MiMo V2.5、不推荐供应商。容量说明区分保守运行配置、2 vCPU 试运行及本机测试局限，部署基线登记已创建但未部署的试用环境。
- 配置/数据迁移：无环境变量或数据契约变化，无程序变更。网络规则仅核对，未变更；来宾系统页面文件、磁盘余量和连通性待部署时验证。
- 部署/回滚：版本元数据统一为 0.11.9，重新构建 ZIP 与 SHA-256，以便首次部署同时获得最新说明；处理逻辑与依赖范围不变，无数据迁移。可按标准脚本回滚至旧版本，不覆盖共享数据。

## 0.11.8 — 2026-09-05

### 2026-09-04 — 修复部署缺口（Node 运行时与自启动入口）并加固网关循环

- 类型：部署脚本修正与运行健壮性（部署影响：安装脚本新增一次性 Node 运行时放置步骤与校验；升级并重启网关后生效）。
- 修改：`install_release.ps1` 校验 `C:\OOPZ\shared\tools\node\node.exe` 存在（缺失时中止安装并给出提示），并把发布目录 `tools\node` 联接到该共享目录——发布包本身不含 Node 运行时，此前按文档部署的服务器在首次 PDF 渲染时必然失败；`README_CLOUD_SERVER_DEPLOYMENT.md` 第 2、4、9 节同步补充 `node.exe` 的放置步骤与目录结构。`启动OOPZ全流程.bat` 补入 `invoke_full_stack_launcher.ps1` 依赖的 `OOPZ full-stack launcher` 标记串（计划任务自启动入口此前必然抛错）。网关侧：serve 循环对 drain/reconcile/cleanup 逐项异常隔离，损坏的 send_requests 条目跳过、被占用的会话文件在保留清理中记录后跳过，不再导致网关进程退出并连带取消进行中的录音。崩溃恢复链路：`oopz-continuous repair` 现在能收敛被中断（interrupted）的会话——此前修复会在转写完成后因缺失 `stopped_at` 必然崩溃，排队分片缺失 lifecycle.json 也会让修复中止。
- 配置/数据迁移：无新环境变量、无数据迁移。已部署服务器需一次性把 Node.js LTS 的 `node.exe` 放入 `C:\OOPZ\shared\tools\node\`，再安装本版本。
- 部署步骤：放置 node.exe → 按正常流程安装发布包 → 重启网关；开机自启动任务无需改动（入口脚本修复后自动恢复）。
- 回滚：回滚代码即可；`shared\tools\node` 目录残留无害。回滚到 0.11.7 及更早版本后，serve 循环与自启动入口的缺陷随之恢复。
- 验证：完整测试 `182 passed, 1 skipped`。

## 0.11.7 — 2026-09-04

### 2026-09-04 — 修复 `.env` 写入破坏发布目录硬链接

- 类型：Bug 修复（部署影响：修复后服务器上的控制群自动绑定、一键配置与群内“设置”写入才能真正持久化到 `shared\config\.env`）。
- 修改：`settings.py` 的 `.env` 写入由“临时文件 + `os.replace`”改为原地截断重写并 `fsync`。原实现会在首次写入时切断 `install_release.ps1` 建立的发布目录 `.env` → `shared\config\.env` 硬链接：受影响写入为首次入群自动绑定（`bind_admin_chat_id`）、`oopz-feishu setup` 的凭据写入和群内“设置”命令；下次升级重建硬链接后这些修改会丢失。
- 配置/数据迁移：无环境变量契约变化。当前生产服务器尚未部署，无存量脱钩数据；若某台服务器已运行过受影响版本并做过上述写入，升级前应人工比对当前版本目录 `.env` 与 `shared\config\.env`，把缺失行合并回 `shared` 版本。
- 部署步骤：按正常流程安装新发布包并重启网关，无需其他操作。`docs/DEPLOYMENT_STATE.md` 已注明 `.env` 写入必须保持原地写，`README_CLOUD_SERVER_DEPLOYMENT.md` 第 7 节已补充一键配置获取凭据的三种方式。
- 回滚：回滚代码即恢复旧写入方式；已正确写入 `shared` 的配置保留。旧版本在服务器上继续存在脱钩风险，不建议长期回滚使用。
- 验证：新增硬链接保持测试（不支持硬链接的文件系统自动跳过）；完整测试 `177 passed, 1 skipped`。

## 0.11.6 — 2026-09-03

### 2026-09-03 — 新增飞书机器人一键配置与 Python 依赖增项

- 类型：新功能与配置入口（部署影响：服务器 Python 依赖集新增 `qrcode` 及其依赖 `colorama`，其余无需额外操作）。
- 运行变化：新增 `oopz-feishu setup` 子命令，通过飞书官方设备注册流（扫码确认）创建或更新机器人应用，在确认页一并申请 11 项应用身份权限、长连接事件（`im.message.receive_v1`、`p2.im.chat.member.bot.added_v1`）与卡片回调（`card.action.trigger`），并把 App ID/Secret 写入本机/共享 `.env`。该命令默认更新 `.env` 中已有应用；把凭据覆盖为另一应用需显式 `--force`。四个公开发布 `OOPZ_FEISHU_PUBLIC_*` 变量仍按原方式手动配置。已有部署不运行 setup 则行为完全不变。
- 配置/数据迁移：无环境变量契约变化，无数据迁移；`.env` 仍只在运行机保存。
- 依赖变化：`pyproject.toml` 的 `feishu` extra 新增 `qrcode>=8,<9`（纯 Python，连带 `colorama`）；Node 依赖不变。
- 部署步骤：安装新发布包（安装脚本按声明范围安装依赖）并重启飞书网关；如需使用一键配置，在服务器以管理员运行 `.\.venv\Scripts\oopz-feishu.exe setup` 并由持有应用管理权限的账号扫码确认。
- 回滚：回滚代码即可恢复旧命令面；虚拟环境中已安装的 `qrcode` 残留无害，无需卸载。setup 写入的 `.env` 凭据与既有格式一致，回滚后仍可被旧版本读取。
- 验证：真实注册端点 `begin` 协议字段校验通过；`tests/test_feishu_setup.py` 12 项自动化覆盖；完整测试 `177 passed, 1 skipped`。

## 0.11.5 — 2026-08-29

### 2026-08-29 — README 第 2 节补充依赖软件官方下载地址

- 类型：文档（部署指南），纯文档变更（部署影响：无额外操作，升级并重启即可）。
- 修改：`README_CLOUD_SERVER_DEPLOYMENT.md` 第 2 节「服务器要求」由无链接软件清单改为带官方下载地址与版本约束的表格（Git for Windows / GitHub CLI / Python 3.12 须 3.12.x / Node.js LTS 经 `npx pnpm@10.15.0` 固定 pnpm / Chrome 或 Edge）。
- 配置/数据迁移：无；不改变代码、依赖、环境变量、启动方式与数据格式。
- 部署步骤：随发布包更新文档并重启飞书网关。
- 回滚：回滚代码即可；不涉及持久数据。
- 验证：发布审计无新增命中。

## 0.11.4 — 2026-08-28

### 2026-08-28 — 对齐云服务器部署指南与发布规划

- 类型：文档（部署指南），纯文档变更（部署影响：无额外操作，升级并重启即可）。
- 修改：`README_CLOUD_SERVER_DEPLOYMENT.md` 与 0.11.3 发布规划对齐：第 6 节示例 Release ID 改为 `v0.11.3-9a64897bc97d`；第 9 节注明 Node 依赖由安装脚本经 `npx pnpm@10.15.0 install --frozen-lockfile` 安装；第 12 节更新流程补齐 release-audit 与 `-SkipTests`，对齐 AGENTS.md 发布规则。
- 配置/数据迁移：无；不改变代码、依赖、环境变量、启动方式与数据格式。
- 部署步骤：随发布包更新文档并重启飞书网关。
- 回滚：回滚代码即可；不涉及持久数据。
- 验证：发布审计无新增命中；完整测试沿用 0.11.3 结果 `164 passed, 2 failed`（2 个为 Windows 沙箱环境测试，非代码回归）。

## 0.11.3 — 2026-08-28（首个生产发布）

### 2026-08-28 — 分析锁释放容错

- 类型：Bug 修复、健壮性（部署影响：升级并重启后生效）。
- 修改：`prepare_analysis`（`analyzer_job.py`）与 `run_analysis`（`analysis_pipeline.py`）的 `finally` 直接 `unlink` 锁文件，若 `unlink` 抛 `OSError`（如被杀软/编辑器占用）会冒泡并令整个分析以失败告终。新增 `_release_lock` 容错辅助（仅记录 warning、不阻断），三处 `finally` 改调用它；`analyzer_job.py` 补充 `LOGGER`。
- 配置/数据迁移：无；`.env`、报告格式、会话与检查点不变。
- 部署步骤：安装发布包后重启飞书网关。
- 回滚：回滚代码即可；不涉及持久数据。
- 验证：lock 相关测试 `18 passed`；完整测试 `164 passed, 2 failed`（2 个失败为本 Windows 沙箱 `rmdir`/symlink 语义差异，与改动无关，目标 Windows Server 应正常）。

### 2026-08-28 — 精简代码并消除保护性编程盲区

- 类型：代码精简、死代码清理、重复实现收编、可观测性（部署影响：升级并重启后生效）。
- 修改：删除全仓零引用死代码（`recovery_guard.py` 整模块及 `workflow.new_request`、`send_request.expedite_pending_send_requests`、`audio_io.write_mono_pcm16`、`analysis_pipeline._compact_turns`、`controller.wait_until_idle` 与随之失效的 9 处导入）；新增 `jsonio.py` 收编分散同构的 JSON 读写；合并同构成本函数；消除 4 处 `except Exception: pass` 沉默吞异常改为日志；修复 `_acquire_run_lock` 漏捕获 `AttributeError`/`OSError`；常量化 `LIVE_CONFIG_FIELDS`；移除恒假死分支。
- 配置/数据迁移：无；不改变依赖、环境变量、启动方式与数据格式。
- 部署步骤：随发布包更新代码并重启飞书网关。
- 回滚：回滚代码即可；不涉及持久数据。
- 验证：编译与导入全通过；完整测试 `164 passed`（2 个环境相关失败同前）。

### 2026-08-27 — 分析异常退出的断点恢复

- 类型：运行可靠性与飞书会话可发现性修复。
- 运行变化：网关启动时检查未完成分析的锁文件；仅锁内 PID 已不存在且路径安全时才回收锁，并把分析生命周期写为“中断可恢复”。恢复入口会重新出现在“待分析”和“删除会话”，状态查询会提示恢复操作；存活 PID 的任务不被抢占。
- 数据与迁移：不改变 `.env`、报告格式或已有检查点。恢复分析复用已落盘的短/长窗口摘要，只请求缺失阶段；无需迁移或清理会话数据。
- 部署步骤：安装发布包后重启飞书网关。重启完成后检查“状态”，再通过“待分析”恢复中断会话；尚未完成的会话在恢复前不会出现在 PDF/完整报告列表。
- 回滚：回滚代码不会删除会话或检查点；但旧版本不会自动释放异常退出遗留的锁，必要时应先停止相关进程再按新版本恢复。
- 验证：覆盖死锁可见与回收、存活锁保护、启动后状态恢复和待分析卡片提示；完整测试 `165 passed, 1 skipped`。

### 2026-08-23 — 分析 API 配置改为全量显式必填

- 类型：生产配置契约与启动校验。
- 运行变化：生产网关启动时必须读取完整的 `ANALYZER_PROVIDER`、API Key、Base URL、模型、超时、重试、请求间隔、普通/思考 Token 上限、思考模式和 JSON 模式；任何一项缺失都会拒绝启动，不再按供应商补全地址或模型，也不再补全运行参数。
- 推荐说明（2026-09-10 同步）：仅推荐 MiMo V2.5，不推荐供应商；配置仍须由使用者填写。300 秒窗口默认 4 路并行不变。
- 配置迁移：部署新版本前，按 `.env.example` 检查服务器持久配置并显式填写全部 11 个 `ANALYZER_*` 项；不得把实际密钥写入仓库或发布包。无需迁移会话、模型或飞书状态数据。
- 部署步骤：先补齐服务器共享 `.env`，再安装发布包并重启网关；通过启动日志和一次受控分析确认所选供应商与模型可用。
- 回滚：回滚应用代码可恢复旧默认行为；建议保留已显式填写的配置，回滚不需要恢复持久数据。
- 验证：分析配置缺项、端点/模型无回退、设置状态与生产启动失败均有测试覆盖；完整测试 `162 passed, 1 skipped`。

### 2026-08-23 — 飞书卡片提示统一为按钮交互

- 类型：运行时交互修复。
- 运行变化：分析确认卡移除“回复：是 / 否”，录音目标卡移除“回复编号”并提供取消按钮；旧控制器文案在飞书出口统一转换。
- 配置与数据迁移：无；`.env`、会话目录和飞书状态格式不变。
- 部署步骤：随下一发布包更新代码并重启飞书网关。
- 回滚：回滚代码即可；不涉及持久数据恢复。
- 验证：自动化覆盖长句适配、Outbox 卡片和选择取消交互；完整测试 `160 passed, 1 skipped`。

### 2026-08-22 — 服务器自动获取开源语音模型

- 类型：紧急部署流程修正，应用版本提升至 `0.11.2`。
- 模型来源：魔搭社区官方 `iic/SenseVoiceSmall`，Apache-2.0；默认固定审核过的模型修订版，不跟随浮动 `master`。
- 安装变化：首次部署由服务器自动下载约 0.94 GB 模型并校验 5 个必需文件的 SHA-256；成功后在共享模型目录保存 `MODEL_SOURCE.json`，后续版本复用。
- 失败策略：模型下载失败、文件缺失或哈希不一致时中止安装，不切换 `current`；已存在但无效的模型目录不会被自动删除或覆盖。
- 发布包校验：构建时统一使用 ZIP 标准 `/` 路径，并强制确认安装脚本、模型下载器和发布清单存在；发现运行目录或敏感内容时拒绝产出正式包。
- 服务器要求：必须允许出站访问 `modelscope.cn`，无需再从开发机复制模型。
- 数据迁移：无。已有且哈希一致的模型只补写来源清单；生产配置、会话数据和飞书状态不变。
- 回滚：代码可回滚；共享模型不随代码回滚。更换模型修订版必须另行提供兼容与恢复说明。
- 验证：固定修订版可由 ModelScope 精确下载；下载器单元测试、完整 pytest、发布审计和新 Release 哈希校验。

### 2026-08-22 — 建立云服务器发布基线

- 类型：部署流程。
- 代码/配置影响：新增基于干净 Git 提交的发布包、SHA-256 校验、版本目录、共享持久目录、健康检查和回滚约定。
- 发布审计：新增 `.codex/release-audit-baseline.json`，复用已人工确认的敏感词误报位置；每次提交/发布生成不含匹配文本的内部审计日志，新命中会阻止发布。
- 部署文档：新增根目录 `README_CLOUD_SERVER_DEPLOYMENT.md`，记录私有 GitHub 认证、Release 下载、持久目录、配置/模型准备、安装、验证、自动启动、更新与回滚的完整 Windows 云服务器流程；纳入飞书机器人从零配置说明。
- 交付方式：正式包使用 GitHub Release 同时发布 ZIP 与 SHA-256；服务器按 Release ID 下载，避免直接跟随可变化的 `main`。
- 服务器操作：首次建立 `C:\OOPZ` 目录结构，准备生产 `.env` 与模型，安装 Python 3.12、Node.js LTS 和 Chrome/Edge。
- 数据迁移：无；首次部署时由服务器下载并校验模型，同时创建空的输出/状态/日志目录。
- 回滚：首次部署无旧版本可回滚；后续版本保留上一发布目录。
- 验证：完整 pytest、发布包哈希、进程存活、飞书长连接日志及一轮端到端业务验证。
- 对应提交：飞书迁移基线 `cdfe7408e8ae011e9897ab95fbde3035754b769a` 与本条更新所在提交。首个 GitHub Release ID 由该提交的 `RELEASE_MANIFEST.json` 记录。

## 已发布

### 0.11.8 — 2026-09-05

- 发布 ID：`v0.11.8-0ebf17f9f464`（[GitHub Release](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.8-0ebf17f9f464)）。
- Git 提交：`0ebf17f9f46496d920bae9c95f9eafda0e0b8d8c`（含全仓复审修复与部署缺口修正）。
- 部署时间：待首次服务器安装后回填。
- 验证：发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 等价检查无新增命中；完整测试 `182 passed, 1 skipped`。
- 依赖变化：无。
- 回滚：回滚应用代码到上一发布目录（0.11.7）即可；持久数据不随代码回滚。`shared\tools\node` 目录残留无害。

### 0.11.7 — 2026-09-04

- 发布 ID：`v0.11.7-7791e58f0359`（[GitHub Release](https://github.com/XK205E3n/OOPZ_Capture/releases/tag/v0.11.7-7791e58f0359)）。
- Git 提交：`7791e58f03595864af9319a6879e04fbaf897adf`（含 `.env` 硬链接修复、测试凭据卫生与部署文档对齐）。
- 部署时间：待首次服务器安装后回填。
- 验证：发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 等价检查无新增命中；完整测试 `177 passed, 1 skipped`。
- 依赖变化：无。
- 回滚：回滚应用代码到上一发布目录（0.11.6）即可；持久数据不随代码回滚。注意 0.11.6 及更早版本在服务器上存在 `.env` 硬链接脱钩风险，不建议长期回滚使用。

### 0.11.6 — 2026-09-03

- 发布 ID：以发布包内 `RELEASE_MANIFEST.json` 的 `release_id` 为准（形如 `v0.11.6-<git_commit_short>`）。
- Git 提交：精确值见发布包内 `RELEASE_MANIFEST.json` 的 `git_commit`（即含一键配置功能、代码精简与结构清理的提交）。
- 部署时间：待首次服务器安装后回填。
- 验证：发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 无新增命中；完整测试 `176 passed, 1 skipped`。
- 依赖变化：`feishu` extra 新增 `qrcode>=8,<9`（连带 `colorama`）；服务器安装发布包时由安装脚本自动安装。
- 回滚：回滚应用代码到上一发布目录（0.11.5）即可；持久数据不随代码回滚，虚拟环境中已安装的 `qrcode` 残留无害。

### 0.11.3 — 2026-08-28（首个生产发布）

- 发布 ID：以发布包内 `RELEASE_MANIFEST.json` 的 `release_id` 为准（形如 `v0.11.3-<git_commit_short>`）。
- Git 提交：精确值见发布包内 `RELEASE_MANIFEST.json` 的 `git_commit`。
- 部署时间：待首次服务器安装后回填。
- 验证：完整 pytest `164 passed, 2 failed`（2 个为 Windows 沙箱环境测试，非代码回归）；发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 无新增命中。
- 回滚：回滚应用代码到上一发布目录即可；持久数据（配置/模型/输出/飞书状态/日志）不随代码回滚。紧急回滚亦可 `git reset --hard pre-slash-20260828`。

### 0.11.4 — 2026-08-28

- 发布 ID：以发布包内 `RELEASE_MANIFEST.json` 的 `release_id` 为准（形如 `v0.11.4-<git_commit_short>`）。
- Git 提交：精确值见发布包内 `RELEASE_MANIFEST.json` 的 `git_commit`（即含 README 部署指南对齐修正的提交）。
- 部署时间：待首次服务器安装后回填。
- 验证：发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 无新增命中。
- 回滚：回滚应用代码到上一发布目录（0.11.3）即可；持久数据不随代码回滚。

### 0.11.5 — 2026-08-29

- 发布 ID：以发布包内 `RELEASE_MANIFEST.json` 的 `release_id` 为准（形如 `v0.11.5-<git_commit_short>`）。
- Git 提交：精确值见发布包内 `RELEASE_MANIFEST.json` 的 `git_commit`（即含 README 第 2 节依赖软件下载地址的提交）。
- 部署时间：待首次服务器安装后回填。
- 验证：发布包 SHA-256 与 `RELEASE_MANIFEST.json` 校验通过；release-audit 无新增命中。
- 回滚：回滚应用代码到上一发布目录（0.11.4）即可；持久数据不随代码回滚。
