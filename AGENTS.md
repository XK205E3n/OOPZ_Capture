# OOPZ Capture 仓库全局规则

本文件适用于整个仓库，所有人工修改与自动化代理修改都必须遵守。

## 部署一致性（强制）

1. 开始修改前阅读 `docs/DEPLOYMENT_STATE.md`；涉及依赖、环境变量、启动方式、目录、系统要求、外部服务、数据迁移或发布步骤时，同时阅读 `docs/DEPLOYMENT.md`。
2. 不得把 `.env`、账号、密钥、服务器地址、私钥、运行数据、日志或模型文件提交到 Git 或放入发布包。
3. 本地与服务器使用同一提交和同一发布包。不得直接在服务器的 `current` 或 `releases` 目录修改程序；紧急修复也必须先回到本地形成提交和新发布包。
4. 任何影响部署的修改必须在同一个提交中：
   - 更新 `.env.example`（如配置契约改变）；
   - 更新 `docs/DEPLOYMENT_STATE.md`（如本地/服务器差异或部署基线改变）；
   - 在 `docs/DEPLOYMENT_CHANGELOG.md` 的“待发布”下登记变更、迁移步骤和回滚影响；
   - 必要时更新 `docs/DEPLOYMENT.md` 和部署脚本。
5. 发布前必须保证工作区干净并运行测试。正式发布只能由 `scripts/build_release.ps1` 从已提交的 `HEAD` 生成；不得复制整个工作目录部署。
6. 提交或发布前必须使用可用的 `release-audit` 技能和 `.codex/release-audit-baseline.json` 执行敏感信息、受保护路径、Git 历史和大文件检查，并将脱敏结果写入被 Git 忽略的 `logs/release_audit/latest.json`。任何新命中都必须人工复核，禁止自动加入误报基线。
7. 服务器持久目录（配置、模型、输出、飞书状态、日志）与版本目录分离。部署和回滚不得覆盖或删除持久数据。
8. 每次生产部署后，将实际版本、时间、结果和服务器特有差异回填到 `docs/DEPLOYMENT_STATE.md`；将对应待发布条目归档为已发布记录。

## 变更原则

- 优先保持 Windows Server 部署路径可用；若新增 Linux 或容器方案，必须作为独立、经过验证的运行目标记录，不能默认为已兼容。
- 配置项应通过环境变量和 `.env.example` 声明，不在代码中写死生产值。
- 数据格式或状态目录发生不兼容变化时，必须提供迁移与回滚说明，并在部署前备份。
